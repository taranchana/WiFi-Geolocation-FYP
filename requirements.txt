# Core dependencies for WiFi GeoMapper Project

# HTTP requests for WiGLE API access
requests>=2.31.0

# Mapping library for visualisation
folium>=0.15.1

# Load API credentials from .env file
python-dotenv>=1.0.1

# Upload server for receiving capture files from remote devices
flask>=3.0.0
